# Committed on purpose: this file holds no secrets, only the local SQLite path,
# so that `npm install && npm run db:setup && npm run dev` works on a clean clone.
# Real secrets belong in .env.local, which is gitignored. See .env.example.
DATABASE_URL="file:./dev.db"
